---
apiVersion: v1
kind: Namespace
metadata:
  name: argocd
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: argocd
  namespace: argocd
spec:
  interval: 12h
  url: https://argoproj.github.io/argo-helm
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: argocd
  namespace: argocd
spec:
  interval: 1h
  timeout: 5m
  releaseName: argo-cd
  targetNamespace: argocd
  chart:
    spec:
      chart: argo-cd
      sourceRef:
        kind: HelmRepository
        name: argocd
      interval: 1m

  # values:
  #   global: 
  #     domain: argocd.example.com

  values:
    configs:
      rbac:
        policy.csv: |
          # Administradores - Acceso completo
          p, role:admin, applications, *, *, allow
          p, role:admin, clusters, *, *, allow
          p, role:admin, repositories, *, *, allow
          p, role:admin, projects, *, *, allow
          p, role:admin, accounts, *, *, allow
          p, role:admin, logs, *, *, allow
          p, role:admin, exec, *, *, allow
          p, role:admin, gpgkeys, *, *, allow
          p, role:admin, certificates, *, *, allow

          # Desarrolladores - Acceso global excepto app-root
          p, role:developer, clusters, get, *, allow
          p, role:developer, projects, get, *, allow
          p, role:developer, applications, get, *, allow
          p, role:developer, applications, sync, *, allow
          p, role:developer, applications, list, *, allow
          p, role:developer, repositories, get, *, allow
          p, role:developer, logs, get, *, allow
          p, role:developer, exec, create, *, allow
          # Denegar acceso específicamente a la app-root
          p, role:developer, applications, get, */apps-config-root, deny
          p, role:developer, applications, sync, */apps-config-root, deny
          p, role:developer, applications, list, */apps-config-root, deny

          # Operadores - Solo pueden ver y sincronizar, no modificar
          p, role:operator, applications, get, *, allow
          p, role:operator, applications, sync, *, allow
          p, role:operator, applications, logs, *, allow
          p, role:operator, projects, get, *, allow
          p, role:operator, repositories, get, *, allow
          p, role:operator, clusters, get, *, allow

          # Lectores - Solo pueden ver
          p, role:readonly, applications, get, *, allow
          p, role:readonly, applications, logs, *, allow
          p, role:readonly, projects, get, *, allow
          p, role:readonly, repositories, get, *, allow
          p, role:readonly, clusters, get, *, allow

          # Asignación de roles a grupos y usuarios
          g, admin, role:admin
          g, developers, role:developer
          g, developer1, role:developer
          g, operators, role:operator
          g, readonly, role:readonly
        policy.default: role:readonly
        policy.matchMode: glob
        scopes: '[groups]'
      cm:
        accounts.developer1: apiKey,login
        accounts.developer2: apiKey,login
        accounts.operator1: apiKey,login
        accounts.readonly1: apiKey,login

  install:
    createNamespace: true
    remediation:
      retries: 3

  upgrade:
    remediation:
      retries: 3